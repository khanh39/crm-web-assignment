<h1>All Contacts</h1>

<ul>
<% Contact.all.each do |contact| %>
  <li><%= contact.first_name %> <%= contact.last_name %> <%= contact.email%> <%= contact.note%></li>
  <a href="/contacts/<%= contact.id %>">go to profile</a>
<% end %>
</ul>
